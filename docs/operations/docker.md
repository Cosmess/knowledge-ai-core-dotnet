# Docker

Serviços atuais do Docker Compose:

```txt
postgres
redis
ollama
```

Executar:

```bash
docker compose up -d
```

Serviços planejados:

```txt
knowledge-api
dev-knowledge-mcp
grafana
prometheus
```
